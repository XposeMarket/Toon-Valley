(() => {
  'use strict';
  const TV=window.ToonValley,Life=window.ToonValleyLife;
  if(!TV||!Life)return;
  const {THREE}=TV,bounds=TV.areaBounds,KEY='toon-valley-indoor-service-quests-v2';
  if(!bounds.cafe||!bounds.library||!bounds.cityHall||!bounds.generalStore||!TV.interiorGroups?.cafe||!TV.interiorGroups?.library||!TV.interiorGroups?.cityHall||!TV.interiorGroups?.generalStore)return;

  const fresh=()=>({day:-1,cafe:{stage:'start',cleared:[],done:false},library:{stage:'start',shelved:[],done:false},cityHall:{stage:'start',delivered:[],done:false},store:{stage:'start',restocked:[],done:false}});
  let state=(()=>{try{const p=JSON.parse(localStorage.getItem(KEY)||'{}');return {...fresh(),...p,cafe:{...fresh().cafe,...(p.cafe||{})},library:{...fresh().library,...(p.library||{})},cityHall:{...fresh().cityHall,...(p.cityHall||{})},store:{...fresh().store,...(p.store||{})}}}catch{return fresh()}})();
  const save=()=>{try{localStorage.setItem(KEY,JSON.stringify(state))}catch(error){console.warn('Unable to save indoor service quests',error)}};
  const day=()=>Life.getState().world.day;

  const carryRoot=new THREE.Group();TV.player.add(carryRoot);
  const busTub=(()=>{const g=new THREE.Group(),box=TV.outlinedMesh(TV.unitBox,TV.mat(0x6aa8b8),1.025);box.scale.set(.82,.34,.56);g.add(box);g.position.set(.7,1.12,.18);g.visible=false;carryRoot.add(g);return g})();
  const bookStack=(()=>{const g=new THREE.Group();[0,1,2].forEach((i)=>{const b=TV.outlinedMesh(TV.unitBox,TV.mat([0xd96666,0x5a91d6,0xe6b84c][i]),1.02);b.scale.set(.5,.11,.35);b.position.y=i*.14;g.add(b)});g.position.set(-.66,1.18,.18);g.visible=false;carryRoot.add(g);return g})();
  const mailSatchel=(()=>{const g=new THREE.Group(),bag=TV.outlinedMesh(TV.unitBox,TV.mat(0x8c5b32),1.025);bag.scale.set(.62,.44,.24);g.add(bag);const flap=new THREE.Mesh(TV.unitBox,TV.materials.cream);flap.scale.set(.48,.08,.26);flap.position.set(0,.24,.02);g.add(flap);g.position.set(.68,1.02,.18);g.visible=false;carryRoot.add(g);return g})();
  const stockCrate=(()=>{const g=new THREE.Group(),crate=TV.outlinedMesh(TV.unitBox,TV.mat(0xc89555),1.025);crate.scale.set(.72,.42,.52);g.add(crate);[[-.24,TV.materials.green],[0,TV.materials.orange],[.24,TV.materials.blue]].forEach(([x,mat])=>{const item=new THREE.Mesh(TV.unitBox,mat);item.scale.set(.15,.28,.15);item.position.set(x,.34,0);g.add(item)});g.position.set(-.7,1.08,.18);g.visible=false;carryRoot.add(g);return g})();

  const cafeStops=[[bounds.cafe.cx-4.2,bounds.cafe.cz+1.8,'window table'],[bounds.cafe.cx,bounds.cafe.cz+2.1,'center table'],[bounds.cafe.cx+4.2,bounds.cafe.cz+1.8,'corner table']];
  const dishStacks=cafeStops.map((p,index)=>{const g=new THREE.Group();for(let i=0;i<3;i++){const plate=new THREE.Mesh(new THREE.CylinderGeometry(.34,.34,.055,12),TV.materials.white);plate.position.y=.82+i*.07;plate.rotation.x=.03*(index-1);g.add(plate)}const cup=new THREE.Mesh(new THREE.CylinderGeometry(.11,.13,.3,10),TV.materials.cream);cup.position.set(.38,1.01,.08);g.add(cup);g.position.set(p[0],0,p[1]);TV.interiorGroups.cafe.add(g);return g});
  TV.registerInteraction({x:bounds.cafe.cx-1.8,z:bounds.cafe.cz-5.25,radius:2.8,area:'cafe',prompt:'Ask Ari about the cafe closing shift',enabled:()=>state.cafe.stage==='start'&&!state.cafe.done,action:()=>{state.cafe.stage='pickup';TV.showToast('☕ Ari: “Could you bus the three dining tables before close? Grab the blue tub, clear every table, then bring it back.”',4);save()}});
  TV.registerInteraction({x:bounds.cafe.cx+2,z:bounds.cafe.cz-5.25,radius:2.6,area:'cafe',prompt:'Collect cafe bus tub',enabled:()=>state.cafe.stage==='pickup'&&!state.cafe.done,action:()=>{state.cafe.stage='clear';busTub.visible=true;TV.showToast('🧺 Bus tub collected. Clear all three dining tables, then return the tub to Ari.',3.2);save()}});
  cafeStops.forEach((p,index)=>TV.registerInteraction({x:p[0],z:p[1],radius:2.3,area:'cafe',prompt:`Clear ${p[2]}`,enabled:()=>state.cafe.stage==='clear'&&!state.cafe.done&&state.cafe.cleared.length===index,action:()=>{state.cafe.cleared.push(index);dishStacks[index].visible=false;Life.emitProgress('help',1,{activity:'cafe-table-clear',table:index});if(state.cafe.cleared.length===cafeStops.length){state.cafe.stage='return';TV.showToast('✨ All three tables are cleared. Bring the full bus tub back to Ari.',3)}else TV.showToast(`🍽️ Table cleared · ${state.cafe.cleared.length}/${cafeStops.length}. Move to the next dining table.`,2.4);save()}}));
  TV.registerInteraction({x:bounds.cafe.cx,z:bounds.cafe.cz-5.25,radius:2.8,area:'cafe',prompt:'Return full bus tub to Ari',enabled:()=>state.cafe.stage==='return'&&!state.cafe.done,action:()=>{state.cafe.stage='done';state.cafe.done=true;busTub.visible=false;Life.addMoney(105,'Cloud Nine Cafe closing shift');Life.emitProgress('help',2,{activity:'cafe-closing-shift-complete'});TV.showToast('☕ Ari: “Dining room reset and ready for tomorrow. Thank you!” +$105',3.6);save()}});

  const shelfStops=[[bounds.library.cx-5.2,bounds.library.cz+2.5,'history shelf'],[bounds.library.cx,bounds.library.cz+3.2,'story shelf'],[bounds.library.cx+5.2,bounds.library.cz+2.5,'nature shelf']];
  const shelfBooks=shelfStops.map((p,index)=>{const g=new THREE.Group();for(let i=0;i<4;i++){const b=TV.outlinedMesh(TV.unitBox,TV.mat([0x6e9bd6,0xd87578,0xe0b65c,0x72b879][(i+index)%4]),1.018);b.scale.set(.14,.52,.34);b.position.set((i-1.5)*.18,.92,0);g.add(b)}g.position.set(p[0],0,p[1]);g.visible=false;TV.interiorGroups.library.add(g);return g});
  TV.registerInteraction({x:bounds.library.cx-1.8,z:bounds.library.cz-5.25,radius:2.8,area:'library',prompt:'Ask Mabel about the return-cart books',enabled:()=>state.library.stage==='start'&&!state.library.done,action:()=>{state.library.stage='pickup';TV.showToast('📚 Mabel: “The return cart is overflowing. Take the book stack, reshelve all three sections, then check back with me.”',4);save()}});
  TV.registerInteraction({x:bounds.library.cx+2,z:bounds.library.cz-5.25,radius:2.6,area:'library',prompt:'Collect library return stack',enabled:()=>state.library.stage==='pickup'&&!state.library.done,action:()=>{state.library.stage='shelve';bookStack.visible=true;TV.showToast('📚 Return stack collected. Reshelve the history, story, and nature sections in order.',3.2);save()}});
  shelfStops.forEach((p,index)=>TV.registerInteraction({x:p[0],z:p[1],radius:2.3,area:'library',prompt:`Reshelve books at ${p[2]}`,enabled:()=>state.library.stage==='shelve'&&!state.library.done&&state.library.shelved.length===index,action:()=>{state.library.shelved.push(index);shelfBooks[index].visible=true;Life.emitProgress('help',1,{activity:'library-reshelve',shelf:index});if(state.library.shelved.length===shelfStops.length){state.library.stage='return';bookStack.visible=false;TV.showToast('📖 All three sections are reshelved. Return to Mabel for final check-in.',3)}else TV.showToast(`📚 Section reshelved · ${state.library.shelved.length}/${shelfStops.length}. Continue to the next shelf.`,2.4);save()}}));
  TV.registerInteraction({x:bounds.library.cx,z:bounds.library.cz-5.25,radius:2.8,area:'library',prompt:'Check completed reshelving with Mabel',enabled:()=>state.library.stage==='return'&&!state.library.done,action:()=>{state.library.stage='done';state.library.done=true;Life.addMoney(115,'Storybook Library reshelving shift');Life.emitProgress('help',2,{activity:'library-reshelving-complete'});TV.showToast('📚 Mabel: “Every return is back where it belongs. Beautiful work!” +$115',3.6);save()}});

  const hallStops=[[bounds.cityHall.cx-4.8,bounds.cityHall.cz+2.2,'records desk'],[bounds.cityHall.cx,bounds.cityHall.cz+3,'permit desk'],[bounds.cityHall.cx+4.8,bounds.cityHall.cz+2.2,'mayor desk']];
  const hallEnvelopes=hallStops.map((p)=>{const e=TV.outlinedMesh(TV.unitBox,TV.materials.cream,1.02);e.scale.set(.52,.06,.36);e.position.set(p[0],.9,p[1]);e.visible=false;TV.interiorGroups.cityHall.add(e);return e});
  TV.registerInteraction({x:bounds.cityHall.cx-1.8,z:bounds.cityHall.cz-5.1,radius:2.8,area:'cityHall',prompt:'Ask Maya about the document round',enabled:()=>state.cityHall.stage==='start'&&!state.cityHall.done,action:()=>{state.cityHall.stage='pickup';TV.showToast('🏛️ Maya: “Three departments need their stamped packets. Take the satchel, hand-deliver each packet, then bring the empty bag back.”',4);save()}});
  TV.registerInteraction({x:bounds.cityHall.cx+2,z:bounds.cityHall.cz-5.1,radius:2.6,area:'cityHall',prompt:'Collect City Hall mail satchel',enabled:()=>state.cityHall.stage==='pickup'&&!state.cityHall.done,action:()=>{state.cityHall.stage='deliver';mailSatchel.visible=true;TV.showToast('📬 Satchel collected. Deliver the packets to records, permits, and the mayor desk in order.',3.2);save()}});
  hallStops.forEach((p,index)=>TV.registerInteraction({x:p[0],z:p[1],radius:2.3,area:'cityHall',prompt:`Deliver packet to ${p[2]}`,enabled:()=>state.cityHall.stage==='deliver'&&!state.cityHall.done&&state.cityHall.delivered.length===index,action:()=>{state.cityHall.delivered.push(index);hallEnvelopes[index].visible=true;Life.emitProgress('help',1,{activity:'city-hall-document-delivery',desk:index});if(state.cityHall.delivered.length===hallStops.length){state.cityHall.stage='return';TV.showToast('✅ All three packets delivered. Return the empty satchel to Maya for sign-off.',3)}else TV.showToast(`📨 Packet delivered · ${state.cityHall.delivered.length}/${hallStops.length}. Continue the office round.`,2.5);save()}}));
  TV.registerInteraction({x:bounds.cityHall.cx,z:bounds.cityHall.cz-5.1,radius:2.8,area:'cityHall',prompt:'Return empty mail satchel to Maya',enabled:()=>state.cityHall.stage==='return'&&!state.cityHall.done,action:()=>{state.cityHall.stage='done';state.cityHall.done=true;mailSatchel.visible=false;Life.addMoney(125,'City Hall document round');Life.emitProgress('help',2,{activity:'city-hall-document-round-complete'});TV.showToast('🏛️ Maya: “Every department signed for its packet. Perfect route!” +$125',3.6);save()}});

  const storeStops=[[bounds.generalStore.cx-4.7,bounds.generalStore.cz+2.25,'produce shelf'],[bounds.generalStore.cx,bounds.generalStore.cz+3,'pantry shelf'],[bounds.generalStore.cx+4.7,bounds.generalStore.cz+2.25,'drinks shelf']];
  const stockDisplays=storeStops.map((p,index)=>{const g=new THREE.Group();for(let i=0;i<3;i++){const box=TV.outlinedMesh(TV.unitBox,TV.mat([0x6fc66b,0xe5a84e,0x5aa4d6][index]),1.018);box.scale.set(.28,.28,.28);box.position.set((i-1)*.34,.88,0);g.add(box)}g.position.set(p[0],0,p[1]);g.visible=false;TV.interiorGroups.generalStore.add(g);return g});
  TV.registerInteraction({x:bounds.generalStore.cx-1.8,z:bounds.generalStore.cz-5.1,radius:2.8,area:'generalStore',prompt:'Ask Cleo about the restock round',enabled:()=>state.store.stage==='start'&&!state.store.done,action:()=>{state.store.stage='pickup';TV.showToast('🛒 Cleo: “Morning delivery is here. Grab the stock crate, refill all three aisles, then bring the empty crate back.”',4);save()}});
  TV.registerInteraction({x:bounds.generalStore.cx+2,z:bounds.generalStore.cz-5.1,radius:2.6,area:'generalStore',prompt:'Collect general store stock crate',enabled:()=>state.store.stage==='pickup'&&!state.store.done,action:()=>{state.store.stage='restock';stockCrate.visible=true;TV.showToast('📦 Stock crate collected. Restock produce, pantry, and drinks in order.',3.2);save()}});
  storeStops.forEach((p,index)=>TV.registerInteraction({x:p[0],z:p[1],radius:2.3,area:'generalStore',prompt:`Restock ${p[2]}`,enabled:()=>state.store.stage==='restock'&&!state.store.done&&state.store.restocked.length===index,action:()=>{state.store.restocked.push(index);stockDisplays[index].visible=true;Life.emitProgress('help',1,{activity:'general-store-restock',shelf:index});if(state.store.restocked.length===storeStops.length){state.store.stage='return';TV.showToast('✨ All three aisles are stocked. Bring the empty crate back to Cleo.',3)}else TV.showToast(`📦 Shelf restocked · ${state.store.restocked.length}/${storeStops.length}. Continue to the next aisle.`,2.5);save()}}));
  TV.registerInteraction({x:bounds.generalStore.cx,z:bounds.generalStore.cz-5.1,radius:2.8,area:'generalStore',prompt:'Return empty stock crate to Cleo',enabled:()=>state.store.stage==='return'&&!state.store.done,action:()=>{state.store.stage='done';state.store.done=true;stockCrate.visible=false;Life.addMoney(110,'Sunny General Store restock round');Life.emitProgress('help',2,{activity:'general-store-restock-complete'});TV.showToast('🛒 Cleo: “Shelves full, crate empty, customers happy!” +$110',3.6);save()}});

  function reset(force=false){const today=day();if(!force&&state.day===today)return;state=fresh();state.day=today;busTub.visible=false;bookStack.visible=false;mailSatchel.visible=false;stockCrate.visible=false;dishStacks.forEach(g=>g.visible=true);shelfBooks.forEach(g=>g.visible=false);hallEnvelopes.forEach(g=>g.visible=false);stockDisplays.forEach(g=>g.visible=false);save()}
  reset();
  state.cafe.cleared.forEach(i=>{if(dishStacks[i])dishStacks[i].visible=false});if(state.cafe.stage==='clear'||state.cafe.stage==='return')busTub.visible=true;
  state.library.shelved.forEach(i=>{if(shelfBooks[i])shelfBooks[i].visible=true});if(state.library.stage==='shelve')bookStack.visible=true;
  state.cityHall.delivered.forEach(i=>{if(hallEnvelopes[i])hallEnvelopes[i].visible=true});if(state.cityHall.stage==='deliver'||state.cityHall.stage==='return')mailSatchel.visible=true;
  state.store.restocked.forEach(i=>{if(stockDisplays[i])stockDisplays[i].visible=true});if(state.store.stage==='restock'||state.store.stage==='return')stockCrate.visible=true;

  let elapsed=0;TV.registerUpdateHook(dt=>{elapsed+=dt;if(elapsed>=2){elapsed=0;if(state.day!==day())reset(true)}});
  function summaries(){return[
    {icon:'☕',title:'Cafe Closing Shift',done:state.cafe.done,status:state.cafe.done?'DONE':state.cafe.stage==='start'?'START':state.cafe.stage==='pickup'?'PICKUP':state.cafe.stage==='return'?'RETURN':`${state.cafe.cleared.length}/${cafeStops.length}`,text:state.cafe.done?'Ari has the full bus tub and the dining room is reset.':state.cafe.stage==='start'?'Talk to Ari inside Cloud Nine Cafe to take the closing shift.':state.cafe.stage==='pickup'?'Collect the blue bus tub from Ari before clearing tables.':state.cafe.stage==='return'?'Bring the full bus tub back to Ari for final handoff and payment.':`Clear the next dining table · ${state.cafe.cleared.length}/${cafeStops.length} complete.`},
    {icon:'📚',title:'Library Reshelving Shift',done:state.library.done,status:state.library.done?'DONE':state.library.stage==='start'?'START':state.library.stage==='pickup'?'PICKUP':state.library.stage==='return'?'CHECK IN':`${state.library.shelved.length}/${shelfStops.length}`,text:state.library.done?'Mabel checked the completed reshelving route.':state.library.stage==='start'?'Talk to Mabel inside Storybook Library about the return cart.':state.library.stage==='pickup'?'Collect the stack of returned books from Mabel.':state.library.stage==='return'?'Return to Mabel for the final reshelving check-in and payment.':`Reshelve the next marked section · ${state.library.shelved.length}/${shelfStops.length} complete.`},
    {icon:'🏛️',title:'City Hall Document Round',done:state.cityHall.done,status:state.cityHall.done?'DONE':state.cityHall.stage==='start'?'START':state.cityHall.stage==='pickup'?'PICKUP':state.cityHall.stage==='return'?'RETURN':`${state.cityHall.delivered.length}/${hallStops.length}`,text:state.cityHall.done?'Maya signed off on the completed document route.':state.cityHall.stage==='start'?'Talk to Maya inside City Hall to take the document round.':state.cityHall.stage==='pickup'?'Collect the mail satchel before starting the office route.':state.cityHall.stage==='return'?'Bring the empty satchel back to Maya for sign-off and payment.':`Hand-deliver the next packet · ${state.cityHall.delivered.length}/${hallStops.length} complete.`},
    {icon:'🛒',title:'General Store Restock',done:state.store.done,status:state.store.done?'DONE':state.store.stage==='start'?'START':state.store.stage==='pickup'?'PICKUP':state.store.stage==='return'?'RETURN':`${state.store.restocked.length}/${storeStops.length}`,text:state.store.done?'Cleo has the empty crate and every aisle is stocked.':state.store.stage==='start'?'Talk to Cleo inside Sunny General Store about the delivery crate.':state.store.stage==='pickup'?'Collect the stock crate before heading down the aisles.':state.store.stage==='return'?'Return the empty crate to Cleo for final check and payment.':`Restock the next aisle · ${state.store.restocked.length}/${storeStops.length} complete.`}
  ]}
  window.ToonValleyIndoorServiceQuests=Object.freeze({getState:()=>JSON.parse(JSON.stringify(state)),getSummaries:summaries,counts:{cafeTables:cafeStops.length,libraryShelves:shelfStops.length,cityHallDesks:hallStops.length,storeShelves:storeStops.length}});
  console.info('Toon Valley indoor service quests ready',{quests:4,cafeTables:cafeStops.length,libraryShelves:shelfStops.length,cityHallDesks:hallStops.length,storeShelves:storeStops.length});
})();
